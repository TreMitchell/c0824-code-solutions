/* The string Tre Mitchell is being assigned to the variable fullName of type string;
The log method of the console object is being called with two arguments, a string &
the variable fullName; The log method of the console object is being called with two
arguments, a string & typeof fullName */
const fullName: string = 'Tré Mitchell';
console.log('value of fullName:', fullName);
console.log('typeof fullName:', typeof fullName);

/* The boolean true is being assigned to the variable isCool of type boolean;
The log method of the console object is being called with two arguments, a string &
the variable isCool; The log method of the console object is being called with two
arguments, a string & typeof isCool */
const isCool: boolean = true;
console.log('value of isCool:', isCool);
console.log('typeof isCool:', typeof isCool);

/* The number 0 is being assigned to the variable totalPets of type number;
The log method of the console object is being called with two arguments, a string &
the variable totalPets; The log method of the console object is being called with two
arguments, a string & typeof totalPets */
const totalPets: number = 0;
console.log('value of totalPets:', totalPets);
console.log('typeof totalPets:', typeof totalPets);

/* Null is being assigned to the variable empty of type null;
The log method of the console object is being called with two arguments, a string &
the variable empty; The log method of the console object is being called with two
arguments, a string & typeof empty */
const empty: null = null;
console.log('value of null:', empty);
console.log('typeof empty:', typeof empty);

/* Undefined is being declared to the variable nothing;
The log method of the console object is being called with two arguments, a string &
the variable nothing; The log method of the console object is being called with two
arguments, a string & typeof nothing */
let nothing: undefined;
console.log('value of nothing:', nothing);
console.log('typeof nothing:', typeof nothing);
