/* exported getAverageOfTwoNumbers */
function getAverageOfTwoNumbers(x: number, y: number): number {
  const average: number = (x + y) / 2;
  return average;
}
console.log(
  `The average of these two numbers are: ${getAverageOfTwoNumbers(2, 4)}`
);
console.log(
  `The average of these two numbers are: ${getAverageOfTwoNumbers(97, 85)}`
);
console.log(
  `The average of these two numbers are: ${getAverageOfTwoNumbers(2, 4)}`
);
